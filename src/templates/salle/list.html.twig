<!DOCTYPE html>
<html>

<body>
Ouais la liste elle est bien
{% if(salles|length > 0) %}
    <ul>
        {% for salle in salles %}
            <li>id: {{ salle.id }}</li>
            <li>Nom: {{ salle.nom }}</li>
            <li>Batiment: {{ salle.batiment }}</li>
            <li>Equipement: {{ salle.equipement }}</li>
            <li>Capacite: {{ salle.capacite }}</li>


        {% endfor %}
    </ul>

{% endif %}

<nav>
    <ul class=pagination">{# verifier si plus de 1 page
                Afficher toutes les pages
                dans une boucle for
        #}
        {% if nbTotalPages > 1 %}
        {% if page == 1 %}
        <a href="">Précédent</a>
        {% else %}
        <a href="/salle/list/{{ page - 1 }}">Précédent</a>
        {% endif %}

        {% for nbPage in 1..nbTotalPages %}
            {% if nbPage == page %}
                <a href="">{{ page }}</a>
            {% else %}
                <a href="/salle/list/{{ nbPage }}">{{ nbPage }}</a>
            {% endif %}
        {% endfor %}


        {% if page == nbTotalPages %}
            <a href="">Suivant</a>
        {% else %}
            <a href="/salle/list/{{ page + 1 }}">Suivant</a>
        {% endif %}
        {% endif %}
        {% if nbTotalPages ==  1 %}
            <a href="/salle/list/1">1</a>
        {% endif %}
    </ul>
</nav>
</body>
</html>
